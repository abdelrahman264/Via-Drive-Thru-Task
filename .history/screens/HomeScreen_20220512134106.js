import React, { useState, useEffect } from "react";
import {
  StyleSheet,
  View,
  ScrollView,
  Text,
  Image,
  TouchableOpacity,
  StatusBar,
  SafeAreaView,
} from "react-native";
import { MaterialIcons, Ionicons } from "@expo/vector-icons";

const HomeScreen = ({ navigation }) => {
  return <Text>Saloom</Text>;
};
const styles = StyleSheet.create({});
export default HomeScreen;
